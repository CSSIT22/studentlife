import React, { FC } from "react"
import { IconType } from "react-icons"

const Buttongroup: FC<{ name:string,con: IconType }> = (props) => {
    return (
        <>
            {
                props.map((item, index) => (

                }) 
            }
        </>
    )
}
export default Buttongroup
